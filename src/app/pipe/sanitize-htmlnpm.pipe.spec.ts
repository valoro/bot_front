import { SanitizeHtmlnpmPipe } from './sanitize-htmlnpm.pipe';

describe('SanitizeHtmlnpmPipe', () => {
  it('create an instance', () => {
    const pipe = new SanitizeHtmlnpmPipe();
    expect(pipe).toBeTruthy();
  });
});
